# 《我们的故事》—— 项目分析报告

> 生成日期：2026-07-29
> 项目类型：手机端 H5 双人纪念空间
> 运行方式：单页面，零依赖，浏览器直接打开（支持 Chrome / Safari / QQ 内置浏览器）

---

## 一、项目定位

私人定制的「双人纪念空间」H5 页面。
不是普通表白网页，定位为**高级数字礼物 / 互动纪念册**。

整体情绪：温暖 · 克制 · 治愈 · 陪伴 · 有故事感。

---

## 二、技术栈

| 层 | 选型 |
|---|---|
| 渲染 | HTML5 + CSS3 + Canvas 2D |
| 动画循环 | requestAnimationFrame（60FPS） |
| 交互 | 原生 JavaScript（零框架） |
| 字体 | -apple-system, PingFang SC, SF Pro Display |
| 依赖 | 纯前端，无需后端 / 无需 npm / 无需 CDN |

---

## 三、项目结构

```
/
├── index.html                 ← 单页入口
├── PROJECT_ANALYSIS.md        ← 本文档
├── css/
│   └── style.css              ← 玻璃拟态 + 动画 + 响应式（10.7KB）
├── js/
│   ├── config.js              ← 所有可配置参数
│   ├── particle.js            ← 粒子系统（核心引擎，10KB）
│   ├── heart.js               ← 爱心呼吸 + 脉冲发光
│   ├── timeline.js            ← 时间轴 + 实时计数器
│   ├── music.js               ← 背景音乐管理
│   ├── video.js               ← 沉浸 MV 播放器
│   ├── easterEgg.js           ← 彩蛋展示模块
│   └── main.js                ← 主控制器（流程编排）
└── assets/
    ├── avatar/     me.jpg + her.jpg     （情侣头像）
    ├── photo/      1.jpg + 2.jpg + 3.png （回忆照片）
    ├── video/      memory.mp4           （回忆视频，竖屏）
    └── music/      music.mp3            （背景音乐）
```

---

## 四、核心功能模块

### 1. 粒子系统（ParticleSystem）

| 元素 | 数量 | 说明 |
|------|------|------|
| 星空粒子 | 80 颗 | 随机位置/大小/速度，闪烁效果 |
| 文字粒子 | 24 条 | 彩色温暖语句，随机 HUE（280°–360°紫红粉色系） |
| 爱心粒子 | 2000 个 | 沿 x=16sin(t)^3, y=13cos(t)-5cos(2t)-2cos(3t)-cos(4t) 参数方程分布 |
| 环绕粒子 | 60 颗 | 绕爱心轨道旋转 |
| 光晕 | 12 个 | 漂浮的径向渐变光斑 |

### 2. 爱心动画（HeartAnimation）

- 呼吸效果：sin 波控制缩放，振幅 8%
- 脉冲光波：心跳节奏扩散
- 闪烁轮廓粒子：30 颗在爱心边缘
- 缓慢旋转

### 3. 时间轴（TimelineManager）

- 2 个时间节点卡片（玻璃拟态设计）
- 相识时间：2026-07-23 04:31
- 情侣关系：2026-07-27
- 实时计数器：天 / 时 / 分 自动跳动

### 4. 沉浸 MV 播放器（VideoManager）

- 无 UI 全屏播放
- 播放前显示过渡文字：「有一段旋律，我想留给你。」
- 播放中叠加温暖文字（渐显渐隐）
- 背景音乐自动暂停 / 恢复

### 5. 背景音乐（MusicManager）

- 自动播放尝试（被浏览器拦截时显示播放按钮）
- 淡入淡出切换
- 循环播放

### 6. 头像展示

- 两张情侣头像从两侧出现
- 缓慢靠近（不重叠，保留视觉间距）
- 光晕光环环绕

### 7. 照片卡片

- 三张照片扇形分布
- 点击可放大查看
- 播放 MV 时隐藏，最终回归时重新显示

---

## 五、完整流程（时间线）

```
[加载画面] ...................... 5秒
  ↓
[文字星海漂浮] ................. 12秒
  24条彩色文字随机漂浮，背景星空闪烁
  ↓
[汇聚成爱心] ................... ~9秒
  文字粒子可见地飘向心形
  ↓
[「我们的故事」标题] ........... 4秒
  ↓
[时间轴] ....................... 10秒
  两张玻璃卡片 + 实时计数器，文字星海继续在背景呼吸
  ↓
[头像展示] ..................... 3.5秒动画 + 8秒停留
  两人头像从两侧出现，缓慢靠近，环绕光晕
  ↓
[照片卡片] ..................... 4秒动画 + 8秒停留
  三张照片扇形浮现，可点击放大
  ↓
[MV 播放] ...................... 视频长度
  头像照片隐藏，背景音乐暂停
  「有一段旋律，我想留给你。」→ 全屏竖屏视频
  ↓ 视频结束 → 音乐恢复
[最终结尾] ..................... 10秒
  「谢谢你来到我的世界。」
  「愿以后的每一天，都能成为新的纪念日。」
  ↓ 淡出
[回归界面] ..................... 持续
  头像 + 照片卡片重新显示，文字星海继续呼吸发光
```

### 时间参数表（config.js 中可调）

| 阶段 | 参数名 | 当前值 |
|------|--------|--------|
| 加载 | loadingDuration | 5000ms |
| 文字漂浮 | floatDuration | 12000ms |
| 汇聚动画 | convergeDuration | 7000ms |
| 标题停留 | heartFormDelay + 4s | ~6s |
| 时间轴 | 硬编码 10s | 10000ms |
| 头像停留 | 硬编码 8s | 8000ms |
| 照片停留 | 硬编码 8s | 8000ms |
| MV过渡文字 | videoIntroDisplay | 3000ms |
| 最终结尾 | finalDisplay | 10000ms |

---

## 六、配置文件说明（config.js）

所有可定制参数集中在 `js/config.js`，修改无需动核心代码：

| 分类 | 内容 |
|------|------|
| timeline | 相识时间、情侣关系时间 |
| loadingText | 加载时显示的文字 |
| messages | 24 条文字粒子内容（可增删） |
| texts | 各阶段文案：标题、时间轴、MV 过渡、最终结尾、视频叠加文字 |
| paths | 头像 / 照片 / 视频 / 音乐的资源路径 |
| particles | 粒子数量、字体大小范围、汇聚速度、呼吸参数 |
| colors | 色调范围、饱和度、明度、爱心颜色 |
| timing | 各阶段持续时间 |
| performance | Retina 适配、帧率目标 |

---

## 七、性能与兼容

- **Canvas Retina 适配**：按 devicePixelRatio（1x ~ 2x）缩放
- **60FPS 动画**：requestAnimationFrame 驱动
- **手机适配**：375px–768px 响应式 + safe-area 全面屏适配
- **浏览器兼容**：Chrome / Safari / QQ 内置浏览器
- **零外部依赖**：无需 npm / CDN，单页面运行，直接文件协议可打开
- **所有 JS 使用 ES5 兼容语法**（var / function / for 循环），无箭头函数/模板字符串/解构赋值，兼容老旧浏览器

---

## 八、改进方向（可对 GPT 提需求）

1. **增加场景切换过渡动画**：阶段切换时添加 Canvas 渐变淡入淡出
2. **音频可视化**：MV 播放时 Canvas 叠加音频频谱动画
3. **交互增强**：滑动查看更多照片 / 点击爱心产生粒子爆炸
4. **性能优化**：根据设备帧率自动降低粒子数
5. **PWA 支持**：添加 manifest.json，支持添加到桌面
6. **多语言**：支持英文 / 其他语言文案切换
7. **自定义背景**：允许上传背景图或视频
8. **背景音乐列表**：支持多首音乐切换
9. **社交分享**：生成分享卡片或链接
10. **云端数据**：连接后端存储，支持远程更新内容

---

## 九、使用方式

1. 把情侣头像放入 `assets/avatar/` 目录
2. 把回忆照片放入 `assets/photo/` 目录
3. 把竖屏回忆视频放入 `assets/video/memory.mp4`
4. 把背景音乐放入 `assets/music/music.mp3`
5. 修改 `js/config.js` 中的时间、文字、颜色等参数
6. 用手机浏览器或 QQ 打开 `index.html`

所有修改只需编辑 `config.js` 一个文件。

---

*End of Report*
